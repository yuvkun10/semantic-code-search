# Copy this file to .env.local for local development.
# Keep .env.local out of git; it may contain account credentials.

# Required for --provider openai and --ai explanations.
OPENAI_API_KEY=

# Optional model overrides.
OPENAI_EMBEDDING_MODEL=text-embedding-3-small
OPENAI_EXPLAIN_MODEL=gpt-5.5
